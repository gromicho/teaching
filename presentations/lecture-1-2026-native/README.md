# Lecture 1, 2026 native Beamer source

Compile from this directory with:

```powershell
lualatex -interaction=batchmode -halt-on-error lecture-1-2026-native.tex
```

The deck uses the shared class and reusable frames in `../abw-shared`. The original PowerPoint is retained unchanged in `references/` as the visual authority. See `CANONICAL-SOURCES.md` for maintenance and attribution records.
